<resources xmlns:tools="http://schemas.android.com/tools">

    <style name="Base.Theme.EventSpot" parent="Theme.Material3.DayNight.NoActionBar">
    </style>

    <style name="Theme.EventSpot" parent="Base.Theme.EventSpot" />

    <style name="LoginTheme" parent="Theme.Material3.DayNight.NoActionBar">
        <item name="android:windowBackground">@color/login_background</item>
        <item name="colorSurface">@color/login_background</item>

        <item name="colorPrimary">@color/login_primary</item>
        <item name="colorOnPrimary">@android:color/white</item>

        <item name="android:textColorPrimary">@color/login_text</item>
        <item name="android:textColorSecondary">@color/login_text_secondary</item>

        <item name="colorControlActivated">@color/login_primary</item>
        <item name="colorControlNormal">@color/login_text</item>

        <item name="android:statusBarColor">@color/login_primary_dark</item>
    </style>

</resources>